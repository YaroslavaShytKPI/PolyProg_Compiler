main{
  // чи парситься true/false????????????????
  //bool y;
  //y = true;

  //y = 2.2;
  // ПОЛІЗ y 1 - 2 3 4 5 / ^ * 6 / + =
  //y = - 1 + 2 * 3 ^ (4 / 5) / 6;

  // ПОЛІЗ y 1.1 2.2 3.3 ^ ^ =
  //y = 1.1 ^ 2.2 ^ 3.3;

  // ПОЛІЗ 1.1 1.1 ^
  //y = 1.1 ^ 1.1;

  //y = 1.1 ^ 1.1 ^ 1.1 ^ 1.1;

  // ПОЛІЗ  y 2 1 / =
  //y = 2/1;

  // ПОЛІЗ  y 2 4 3 9 / * + =
  //y = 2 + 4 * (3 / 9);

  // ПОЛІЗ  y 1 2 3 * 4 / + =
  //y = 1 + 2 * 3 / 4;


  int x;
  //readline(x);
  x = 3;

  // ПОЛІЗ  x 2 - 1 + =
  //x = -2 + 1;

  // ПОЛІЗ  x 1 2 - 4 + + =
  //x = 1 + (-2 + 4);

  // ПОЛІЗ  x 1 2 * =
  //x = 1 * 2;

  // ПОЛІЗ  y 2 4 3 9 / * + =
  // y = 2 + 4 * (3 / 9);

  // ПОЛІЗ  y 1 2 3 * 4 / + =    
  // y = 1 + 2 * 3 / 4;
  
  // ПОЛІЗ  x 1 1 + 1 + =
  // x = 1 + 1 + 1;

  //for (x = 0; x < y + 6; x = x + 1) {
  //  x = 3 ^ 3 + x;
  //  print(x);
  //}

  //while (x + 4 > 0) {
  //  print(x);
  //}

  //if (y + 6.5 < 5) {
  //  y = y + 4.4;
  //}

  if (x + 2 > 3){
    print(x);
  }
  else{
    print(2);
  }
}