main{
  double y;
  y = - 1 + 2 * 3 ^ (4 / 5) / 6;
  int x;
  readline(x, y);
  x = 3;
  ///////////////////// СТЕПІНЬ
  // ПОЛІЗ  x 2 - 1 + =
  // x = -2 + 1;

  // ПОЛІЗ  x 1 2 - 4 + + =
  // x = 1 + (-2 + 4);

  // ПОЛІЗ  x 1 2 * =
  // x = 1 * 2;

  // ПОЛІЗ  y 2 1 / =
  // y = 2/1;

  // ПОЛІЗ  y 2 4 3 9 / * + =
  // y = 2 + 4 * (3 / 9);

  // ПОЛІЗ  y 1 2 3 * 4 / + =    
  // y = 1 + 2 * 3 / 4;
  
  // ПОЛІЗ  x 1 1 + 1 + =
  // x = 1 + 1 + 1;

  //while (x + 4 > 0) {
  //  readline(x);
  //}

  if(x + 2 > 3){
    print(x);
  }else{
    print(2);
  }

}