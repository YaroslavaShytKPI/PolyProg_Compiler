main {
    //////////////////////

    double a, b, c, d;
    // double a;

    a = 1.0;
    b = 8.9;
    c = a + b;

    a = 1.0 + 4.0;
    a = 4.0 - 1.0; 
    a = 1.0 * 3.0;
    a = 1.0 / 3.0;
    a = 1.0 ^ 1.0;

    a = 1 ^ 1;
    a = 1 / 1;

    a = 1 + 1.0;
    a = 1.9 + 2;
    a = 1 + 2.0 * 8 + (9 + 8);
    a = 1 * (5.8 + 9.0);
    a = (5.9 + 9) / 8;
    a = 1 + 4 * 9 / 4.0;
    
    // a = 1 + 1;
    // a = true;
    //a = 9 / true;
    // a = 9 / 0; 
    // a = 1 + (6 * 4 / 0);  
    // c = 3;
    // c = a + b + d;
    //c = x + y;

    ////////////////////////////

    int i, g, k;
    // int i;

    i = 1;
    g = 2;
    k = i + g;

    i = 1 + 1;
    i = 2 - 2;
    i = 3 * 3;
    i = 2 + 3 * 8 - 9;

    // i = 1.0;
    // i = 4 / 4;
    // i = 5 ^ 5;
    // i = 3.4 + 5;

    ////////////////////////////

    bool t, f, n;
    t = true;
    f = false;
    
    if (t > f){}
    while (t == f){}

    // print(t + f);
    // if(t < n){}
    // t = 1;
    // f = 9.9 - 8.9;
}

